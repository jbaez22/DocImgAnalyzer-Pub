project_name = "img-analyzer"
environment  = "prod"
aws_region   = "us-east-1"
owner        = "jose"

# -- Fill these in after starting the "Connect AWS account" flow in Grafana
# Cloud (Connections -> Amazon CloudWatch integration). Grafana Cloud
# generates both values for your org; do not guess or copy example values
# from documentation.
grafana_aws_account_id = "REPLACE_WITH_GRAFANA_AWS_ACCOUNT_ID"
grafana_external_id    = "REPLACE_WITH_GRAFANA_EXTERNAL_ID"
