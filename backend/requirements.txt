# Run `pip-compile requirements.txt --generate-hashes` to produce a fully pinned
# lockfile with hashes for production deployments.

fastapi==0.139.0
mangum==0.19.0
pydantic==2.10.3
boto3==1.35.81
httpx==0.28.1
python-json-logger==2.0.7
starlette==1.3.1
stripe==12.2.0
PyJWT[crypto]==2.13.0
PyYAML==6.0.2
